<div class="-m-2 text-center">
  <% if alert_type == "notice" %>
    <div class="p-2">
      <div class="inline-flex items-center bg-white leading-none text-green-600 rounded-full p-2 shadow text-teal text-sm">
        <span class="inline-flex bg-green-600 text-white rounded-full h-6 px-3 justify-center items-center">Merci 💚</span>
        <span class="inline-flex px-2"><%= message %></span>
      </div>
    </div>
  <% else %>
    <div class="p-2">
      <div class="inline-flex items-center bg-white leading-none text-purple-600 rounded-full p-2 shadow text-sm">
        <span class="inline-flex bg-purple-600 text-white rounded-full h-6 px-3 justify-center items-center text-">Oops</span>
        <span class="inline-flex px-2"><%= message %></span>
      </div>
    </div>
  <% end %>
</div>
